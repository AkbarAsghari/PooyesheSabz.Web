@page "/Pages/Edit/{PageId:guid}"

@attribute [Authorize(Roles = "Admin,Writer")]

@if (existPage == null)
{
    <MudText>در حال بارگذاری</MudText>
}
else
{
    <PageGeneratorForm Page=existPage OnValidSubmit=Update Title="ایجاد صفحه جدید"></PageGeneratorForm>
}